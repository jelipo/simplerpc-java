pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
}
rootProject.name = "simplerpc-java"
include("simplerpc", "main", "simplerpc-netty")

